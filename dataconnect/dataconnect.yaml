specVersion: "v1beta"
serviceId: "firstwebproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firstwebproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
